generated: '2026-07-22'
method: derived
source: openapi/ servers[] (450 spec files declare the sandbox server)
environments:
- name: Production
  url: https://api.factset.com
- name: Sandbox
  url: https://api-sandbox.factset.com
  note: Declared as the "Sandbox" server alongside production in 450 of the
    FactSet OpenAPI documents in this repo; same paths and auth model as
    production. Access is provisioned through the FactSet developer portal /
    account team — FactSet publishes no anonymous self-serve sandbox keys.
test_data:
  note: FactSet publishes no magic test identifiers, test cards, or fixture
    values in its public docs; sandbox behavior is driven by entitlement on the
    sandbox host. Nothing further captured to avoid fabrication.
separation:
  mechanism: host-based (api.factset.com vs api-sandbox.factset.com)
  key_prefixes: null
