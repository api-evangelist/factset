{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "calculation",
  "type": "boolean",
  "description": "This parameter would return the PAI data based on the input provided. All the data for indicators or subtopics requested are returned when the input is `true` and the SFDR required metrics are only returned when the input is`false`\n"
}