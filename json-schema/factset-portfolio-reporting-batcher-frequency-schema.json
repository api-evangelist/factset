{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "frequency",
  "type": "string",
  "description": "Used for ADF job types only. Will determine the frequency between the start and end dates."
}