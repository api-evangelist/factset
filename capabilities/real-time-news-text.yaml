naftiko: 1.0.0-alpha2
info:
  label: FactSet Real-Time News API — Text
  description: 'FactSet Real-Time News API — Text. 1 operations. Lead operation: Factset Search for News Articles Using a
    Fulltext Search.. Self-contained Naftiko capability covering one Factset business surface.'
  tags:
  - Factset
  - Text
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FACTSET_API_KEY: FACTSET_API_KEY
capability:
  consumes:
  - type: http
    namespace: real-time-news-text
    baseUri: https://api.factset.com/wealth/v3
    description: FactSet Real-Time News API — Text business capability. Self-contained, no shared references.
    resources:
    - name: news-article-searchByText
      path: /news/article/searchByText
      operations:
      - name: postnewsarticlesearchbytext
        method: POST
        description: Factset Search for News Articles Using a Fulltext Search.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.FACTSET_API_KEY}}'
  exposes:
  - type: rest
    namespace: real-time-news-text-rest
    port: 8080
    description: REST adapter for FactSet Real-Time News API — Text. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/news/article/searchbytext
      name: news-article-searchbytext
      description: REST surface for news-article-searchByText.
      operations:
      - method: POST
        name: postnewsarticlesearchbytext
        description: Factset Search for News Articles Using a Fulltext Search.
        call: real-time-news-text.postnewsarticlesearchbytext
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: real-time-news-text-mcp
    port: 9090
    transport: http
    description: MCP adapter for FactSet Real-Time News API — Text. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: factset-search-news-articles-using
      description: Factset Search for News Articles Using a Fulltext Search.
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: real-time-news-text.postnewsarticlesearchbytext
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
