naftiko: 1.0.0-alpha2
info:
  label: FactSet Global Prices API — Status
  description: 'FactSet Global Prices API — Status. 1 operations. Lead operation: Factset Returns the Status for a Batch Request.
    Self-contained Naftiko capability covering one Factset business surface.'
  tags:
  - Factset
  - Status
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FACTSET_API_KEY: FACTSET_API_KEY
capability:
  consumes:
  - type: http
    namespace: global-prices-status
    baseUri: https://api.factset.com/content
    description: FactSet Global Prices API — Status business capability. Self-contained, no shared references.
    resources:
    - name: factset-global-prices-v1-batch-status
      path: /factset-global-prices/v1/batch-status
      operations:
      - name: getbatchstatus
        method: GET
        description: Factset Returns the Status for a Batch Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: basic
      username: '{{env.FACTSET_USER}}'
      password: '{{env.FACTSET_PASS}}'
  exposes:
  - type: rest
    namespace: global-prices-status-rest
    port: 8080
    description: REST adapter for FactSet Global Prices API — Status. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/factset-global-prices/v1/batch-status
      name: factset-global-prices-v1-batch-status
      description: REST surface for factset-global-prices-v1-batch-status.
      operations:
      - method: GET
        name: getbatchstatus
        description: Factset Returns the Status for a Batch Request
        call: global-prices-status.getbatchstatus
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: global-prices-status-mcp
    port: 9090
    transport: http
    description: MCP adapter for FactSet Global Prices API — Status. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: factset-returns-status-batch-request
      description: Factset Returns the Status for a Batch Request
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: global-prices-status.getbatchstatus
      outputParameters:
      - type: object
        mapping: $.
