naftiko: 1.0.0-alpha2
info:
  label: Factset PA Engine API — Grouping
  description: 'Factset PA Engine API — Grouping. 1 operations. Lead operation: Factset Get Pa Grouping Frequencies. Self-contained
    Naftiko capability covering one Factset business surface.'
  tags:
  - Factset
  - Grouping
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FACTSET_API_KEY: FACTSET_API_KEY
capability:
  consumes:
  - type: http
    namespace: pa-engine-grouping
    baseUri: https://api.factset.com
    description: Factset PA Engine API — Grouping business capability. Self-contained, no shared references.
    resources:
    - name: analytics-engines-pa-v3-grouping-frequencies
      path: /analytics/engines/pa/v3/grouping-frequencies
      operations:
      - name: getpagroupingfrequencies
        method: GET
        description: Factset Get Pa Grouping Frequencies
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.FACTSET_API_KEY}}'
  exposes:
  - type: rest
    namespace: pa-engine-grouping-rest
    port: 8080
    description: REST adapter for Factset PA Engine API — Grouping. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/analytics/engines/pa/v3/grouping-frequencies
      name: analytics-engines-pa-v3-grouping-frequencies
      description: REST surface for analytics-engines-pa-v3-grouping-frequencies.
      operations:
      - method: GET
        name: getpagroupingfrequencies
        description: Factset Get Pa Grouping Frequencies
        call: pa-engine-grouping.getpagroupingfrequencies
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pa-engine-grouping-mcp
    port: 9090
    transport: http
    description: MCP adapter for Factset PA Engine API — Grouping. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: factset-get-pa-grouping-frequencies
      description: Factset Get Pa Grouping Frequencies
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: pa-engine-grouping.getpagroupingfrequencies
      outputParameters:
      - type: object
        mapping: $.
