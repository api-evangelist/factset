naftiko: 1.0.0-alpha2
info:
  label: FactSet Vault API — Result
  description: 'FactSet Vault API — Result. 1 operations. Lead operation: Factset Get Vault Calculation Result by Id. Self-contained
    Naftiko capability covering one Factset business surface.'
  tags:
  - Factset
  - Result
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FACTSET_API_KEY: FACTSET_API_KEY
capability:
  consumes:
  - type: http
    namespace: vault-result
    baseUri: https://api.factset.com
    description: FactSet Vault API — Result business capability. Self-contained, no shared references.
    resources:
    - name: analytics-engines-vault-v3-calculations-id-units-unitId-result
      path: /analytics/engines/vault/v3/calculations/{id}/units/{unitId}/result
      operations:
      - name: getcalculationunitresultbyid
        method: GET
        description: Factset Get Vault Calculation Result by Id
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: from url, provided from the location header in the Get Vault calculation status by id endpoint
          required: true
        - name: unitId
          in: path
          type: string
          description: from url, provided from the location header in the Get Vault calculation status by id endpoint
          required: true
    authentication:
      type: bearer
      token: '{{env.FACTSET_API_KEY}}'
  exposes:
  - type: rest
    namespace: vault-result-rest
    port: 8080
    description: REST adapter for FactSet Vault API — Result. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/analytics/engines/vault/v3/calculations/{id}/units/{unitid}/result
      name: analytics-engines-vault-v3-calculations-id-units-unitid-result
      description: REST surface for analytics-engines-vault-v3-calculations-id-units-unitId-result.
      operations:
      - method: GET
        name: getcalculationunitresultbyid
        description: Factset Get Vault Calculation Result by Id
        call: vault-result.getcalculationunitresultbyid
        with:
          id: rest.id
          unitId: rest.unitId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: vault-result-mcp
    port: 9090
    transport: http
    description: MCP adapter for FactSet Vault API — Result. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: factset-get-vault-calculation-result
      description: Factset Get Vault Calculation Result by Id
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: vault-result.getcalculationunitresultbyid
      with:
        id: tools.id
        unitId: tools.unitId
      outputParameters:
      - type: object
        mapping: $.
