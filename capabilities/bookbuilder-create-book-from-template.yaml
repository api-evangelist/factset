naftiko: 1.0.0-alpha2
info:
  label: Factset BookBuilder — Create Book From Template
  description: 'Factset BookBuilder — Create Book From Template. 1 operations. Lead operation: Factset Kicks off request to
    create a book with template. Self-contained Naftiko capability covering one Factset business surface.'
  tags:
  - Factset
  - Create Book From Template
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FACTSET_API_KEY: FACTSET_API_KEY
capability:
  consumes:
  - type: http
    namespace: bookbuilder-create-book-from-template
    baseUri: https://api.factset.com/book-builder-api
    description: Factset BookBuilder — Create Book From Template business capability. Self-contained, no shared references.
    resources:
    - name: v1-create-book-from-template
      path: /v1/create-book-from-template
      operations:
      - name: post
        method: POST
        description: Factset Kicks off request to create a book with template
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: basic
      username: '{{env.FACTSET_USER}}'
      password: '{{env.FACTSET_PASS}}'
  exposes:
  - type: rest
    namespace: bookbuilder-create-book-from-template-rest
    port: 8080
    description: REST adapter for Factset BookBuilder — Create Book From Template. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/v1/create-book-from-template
      name: v1-create-book-from-template
      description: REST surface for v1-create-book-from-template.
      operations:
      - method: POST
        name: post
        description: Factset Kicks off request to create a book with template
        call: bookbuilder-create-book-from-template.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bookbuilder-create-book-from-template-mcp
    port: 9090
    transport: http
    description: MCP adapter for Factset BookBuilder — Create Book From Template. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: factset-kicks-off-request-create
      description: Factset Kicks off request to create a book with template
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bookbuilder-create-book-from-template.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
