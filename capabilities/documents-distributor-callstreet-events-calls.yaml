naftiko: 1.0.0-alpha2
info:
  label: FactSet Documents Distributor - CallStreet Events — Calls
  description: 'FactSet Documents Distributor - CallStreet Events — Calls. 1 operations. Lead operation: Factset Returns the
    Active Calls Happening at the Moment.. Self-contained Naftiko capability covering one Factset business surface.'
  tags:
  - Factset
  - Calls
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FACTSET_API_KEY: FACTSET_API_KEY
capability:
  consumes:
  - type: http
    namespace: documents-distributor-callstreet-events-calls
    baseUri: https://api.factset.com
    description: FactSet Documents Distributor - CallStreet Events — Calls business capability. Self-contained, no shared
      references.
    resources:
    - name: bulk-documents-nrt-v1-calls
      path: /bulk-documents/nrt/v1/calls
      operations:
      - name: getbulkdocumentsnrtv1calls
        method: GET
        description: Factset Returns the Active Calls Happening at the Moment.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.FACTSET_API_KEY}}'
  exposes:
  - type: rest
    namespace: documents-distributor-callstreet-events-calls-rest
    port: 8080
    description: REST adapter for FactSet Documents Distributor - CallStreet Events — Calls. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/bulk-documents/nrt/v1/calls
      name: bulk-documents-nrt-v1-calls
      description: REST surface for bulk-documents-nrt-v1-calls.
      operations:
      - method: GET
        name: getbulkdocumentsnrtv1calls
        description: Factset Returns the Active Calls Happening at the Moment.
        call: documents-distributor-callstreet-events-calls.getbulkdocumentsnrtv1calls
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: documents-distributor-callstreet-events-calls-mcp
    port: 9090
    transport: http
    description: MCP adapter for FactSet Documents Distributor - CallStreet Events — Calls. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: factset-returns-active-calls-happening
      description: Factset Returns the Active Calls Happening at the Moment.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: documents-distributor-callstreet-events-calls.getbulkdocumentsnrtv1calls
      outputParameters:
      - type: object
        mapping: $.
