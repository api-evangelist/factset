naftiko: 1.0.0-alpha2
info:
  label: FactSet Prices API — High
  description: 'FactSet Prices API — High. 2 operations. Lead operation: Factset Gets the Price High and Price Low of Securities
    for a List of `ids` as of Given Date, Period and Frequency.. Self-contained Naftiko capability covering one Factset business
    surface.'
  tags:
  - Factset
  - High
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FACTSET_API_KEY: FACTSET_API_KEY
capability:
  consumes:
  - type: http
    namespace: prices-high
    baseUri: https://api.factset.com/content
    description: FactSet Prices API — High business capability. Self-contained, no shared references.
    resources:
    - name: factset-prices-v1-high-low
      path: /factset-prices/v1/high-low
      operations:
      - name: gethighlow
        method: GET
        description: Factset Gets the Price High and Price Low of Securities for a List of `ids` as of Given Date, Period
          and Frequency.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: gethighlowforlist
        method: POST
        description: Factset Requests the Price High and Price Low of Securities for a List of `ids` as of Given Date, Period
          and Frequency.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: basic
      username: '{{env.FACTSET_USER}}'
      password: '{{env.FACTSET_PASS}}'
  exposes:
  - type: rest
    namespace: prices-high-rest
    port: 8080
    description: REST adapter for FactSet Prices API — High. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/factset-prices/v1/high-low
      name: factset-prices-v1-high-low
      description: REST surface for factset-prices-v1-high-low.
      operations:
      - method: GET
        name: gethighlow
        description: Factset Gets the Price High and Price Low of Securities for a List of `ids` as of Given Date, Period
          and Frequency.
        call: prices-high.gethighlow
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: gethighlowforlist
        description: Factset Requests the Price High and Price Low of Securities for a List of `ids` as of Given Date, Period
          and Frequency.
        call: prices-high.gethighlowforlist
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: prices-high-mcp
    port: 9090
    transport: http
    description: MCP adapter for FactSet Prices API — High. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: factset-gets-price-high-and
      description: Factset Gets the Price High and Price Low of Securities for a List of `ids` as of Given Date, Period and
        Frequency.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: prices-high.gethighlow
      outputParameters:
      - type: object
        mapping: $.
    - name: factset-requests-price-high-and
      description: Factset Requests the Price High and Price Low of Securities for a List of `ids` as of Given Date, Period
        and Frequency.
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: prices-high.gethighlowforlist
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
