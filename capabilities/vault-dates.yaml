naftiko: 1.0.0-alpha2
info:
  label: FactSet Vault API — Dates
  description: 'FactSet Vault API — Dates. 1 operations. Lead operation: Factset Convert Vault Dates to Absolute Format. Self-contained
    Naftiko capability covering one Factset business surface.'
  tags:
  - Factset
  - Dates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FACTSET_API_KEY: FACTSET_API_KEY
capability:
  consumes:
  - type: http
    namespace: vault-dates
    baseUri: https://api.factset.com
    description: FactSet Vault API — Dates business capability. Self-contained, no shared references.
    resources:
    - name: analytics-engines-vault-v3-dates
      path: /analytics/engines/vault/v3/dates
      operations:
      - name: convertvaultdatestoabsoluteformat
        method: GET
        description: Factset Convert Vault Dates to Absolute Format
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: startdate
          in: query
          type: string
          description: Start Date
        - name: enddate
          in: query
          type: string
          description: End Date
          required: true
        - name: componentid
          in: query
          type: string
          description: Vault Component Id
          required: true
        - name: account
          in: query
          type: string
          description: Account
          required: true
    authentication:
      type: bearer
      token: '{{env.FACTSET_API_KEY}}'
  exposes:
  - type: rest
    namespace: vault-dates-rest
    port: 8080
    description: REST adapter for FactSet Vault API — Dates. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/analytics/engines/vault/v3/dates
      name: analytics-engines-vault-v3-dates
      description: REST surface for analytics-engines-vault-v3-dates.
      operations:
      - method: GET
        name: convertvaultdatestoabsoluteformat
        description: Factset Convert Vault Dates to Absolute Format
        call: vault-dates.convertvaultdatestoabsoluteformat
        with:
          startdate: rest.startdate
          enddate: rest.enddate
          componentid: rest.componentid
          account: rest.account
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: vault-dates-mcp
    port: 9090
    transport: http
    description: MCP adapter for FactSet Vault API — Dates. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: factset-convert-vault-dates-absolute
      description: Factset Convert Vault Dates to Absolute Format
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: vault-dates.convertvaultdatestoabsoluteformat
      with:
        startdate: tools.startdate
        enddate: tools.enddate
        componentid: tools.componentid
        account: tools.account
      outputParameters:
      - type: object
        mapping: $.
