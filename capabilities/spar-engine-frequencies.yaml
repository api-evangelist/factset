naftiko: 1.0.0-alpha2
info:
  label: FactSet SPAR Engine API — Frequencies
  description: 'FactSet SPAR Engine API — Frequencies. 1 operations. Lead operation: Factset Get Spar Frequencies. Self-contained
    Naftiko capability covering one Factset business surface.'
  tags:
  - Factset
  - Frequencies
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FACTSET_API_KEY: FACTSET_API_KEY
capability:
  consumes:
  - type: http
    namespace: spar-engine-frequencies
    baseUri: https://api.factset.com
    description: FactSet SPAR Engine API — Frequencies business capability. Self-contained, no shared references.
    resources:
    - name: analytics-engines-spar-v3-frequencies
      path: /analytics/engines/spar/v3/frequencies
      operations:
      - name: getsparfrequencies
        method: GET
        description: Factset Get Spar Frequencies
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.FACTSET_API_KEY}}'
  exposes:
  - type: rest
    namespace: spar-engine-frequencies-rest
    port: 8080
    description: REST adapter for FactSet SPAR Engine API — Frequencies. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/analytics/engines/spar/v3/frequencies
      name: analytics-engines-spar-v3-frequencies
      description: REST surface for analytics-engines-spar-v3-frequencies.
      operations:
      - method: GET
        name: getsparfrequencies
        description: Factset Get Spar Frequencies
        call: spar-engine-frequencies.getsparfrequencies
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: spar-engine-frequencies-mcp
    port: 9090
    transport: http
    description: MCP adapter for FactSet SPAR Engine API — Frequencies. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: factset-get-spar-frequencies
      description: Factset Get Spar Frequencies
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: spar-engine-frequencies.getsparfrequencies
      outputParameters:
      - type: object
        mapping: $.
